spring:
  datasource:
    url: jdbc:mysql://localhost:3306/APPOITMENT_SYSTEM
    username: root
    password: gospodariki
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update

  security:
    jwt:
      secret: mygeneratedsecretkeyforalgorithmHS256springsecurity
      expiration: 3600000

  server:
    port: 8080

logging:
  level:
    org.springframework.security: DEBUG
